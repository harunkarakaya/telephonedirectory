@model TelefonRehberi.Entities.Departman

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>
<div class="row">
    <div class="col-md-4 col-md-offset-4 col-sm-4 col-sm-offset-4">
        <h3 class="text-primary">Bu departmanı silmek istediğinizden emin misiniz?</h3>
        <div>
            <hr />
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.DepartmanAdi)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.DepartmanAdi)
                </dd>

            </dl>

            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()

                <div class="form-actions no-color">
                    <input type="submit" value="Sil" class="btn btn-danger" /> |
                    <a href="/Departman/Index" class="btn btn-default btn-sm"><span class="glyphicon glyphicon-arrow-left"></span> Geri Dön</a>
                </div>
            }
        </div>
    </div>
</div>
